// Imports the Flutter Driver API

// Imports the Flutter Driver API

void main() {
//  group('scrolling performance test', () {
//    FlutterDriver driver;
//
//    setUpAll(() async {
//      // Connects to the app
//      driver = await FlutterDriver.connect();
//    });
//
//    tearDownAll(() async {
//      if (driver != null) {
//        // Closes the connection
//        driver.close();
//      }
//    });
//
//    test('takescreeshots', () async {
//      driver.waitUntilNoTransientCallbacks();
//      List<int> screenshot = await driver.screenshot();
//      const String screenshotDir = '../screenshots/';
//      print(screenshotDir);
////      File imgFile = new File('$screeshotDir/screenshot.png');
////      imgFile.createSync(recursive: true);
////      imgFile.writeAsBytesSync(screenshot);
//    });
//  });
}
